{% extends 'base.html.twig' %}

{% block title %}Modifier le trick "{{ trick.name }}"{% endblock %}

{% block body %}
    {% include 'header.twig' %}
    <div class="container">
        <h1 class="pixelify-sans-font fs-2">Modifier le trick "{{ trick.name }}"</h1>
        <div class="row">
            <div class="col col-12 col-md-8 mx-auto">
                {% include 'trick/_form.html.twig' %}

            </div>
        </div>
    </div>
{% endblock %}
